# Backblaze Status Monitor Requirements
# Python standard library only - no external dependencies required
# This application uses only built-in Python modules:
# - tkinter (GUI)
# - subprocess (running commands)
# - xml.etree.ElementTree (parsing XML)
# - threading (background updates)
# - pathlib (file path handling)
# - re (regular expressions)

# Optional: If you want to package the application
# pyinstaller>=5.0  # For creating standalone executable
